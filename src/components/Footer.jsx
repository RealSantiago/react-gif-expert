import { Grid, Typography } from "@mui/material";
import React from "react";

export const Footer = () => {
  return (
    <Grid container justifyContent={"center"} bottom={0} left={0}>
      <Typography>@Hi</Typography>
    </Grid>
  );
};
